# QIIME2
target-files: # still in progress
  '"artifacts/feature-table.tsv", "bray-distance-matrix/distance_matrix.tsv", "artifacts/taxa-barplot.qzv"'

manifest: 
  "data/paired_end_manifest.tsv"
metadata:
  "data/paired_end_metadata.tsv"

tools:
  #"DADA2"
  Qiime2:
    #- "artifacts/taxonomy.qza"
    - "artifacts/unweighted-unifrac-pcoa-matrix.qza"
  #Mothur:
  #Pathoscope2:
  #Kraken:

classifier:
  classifier/silva-138-99-nb-classifier.qza

table-filter:
  "chloroplast,mitochondria,eukaryota,unassigned"

beta-div-column:
  "Groups"

# automation algorithm in the future for this
trunclenf:
  "290"
trunclenr:
  "280"
trimleftf:
  "10"
trimleftr:
  "15"
# automated trimming parameters
min-quality:
  "20"
quality-window:
  "10"
min-length-fraction:
  "0.75"
max-ambiguous:
  "0"

# automation in progress
sampling_depth:
  "1103"
sampling_depth_phylogenetic:
  "146159"